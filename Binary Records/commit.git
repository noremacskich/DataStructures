Input Sanitization

Modified:
1	main(void)

Reasons:
1	The inputs are now sanitized.  

Fixed:
1	Sanitize Inputs
2	Sort Index
5	when using arrow keys on input, program goes into infinite loop

Todo:
1	Clean up Code
2	Recheck requirements
3	Should find way of keeping on same screen when the input is wrong.

Note:
1	Forgot to include Sort Index for the last commit.

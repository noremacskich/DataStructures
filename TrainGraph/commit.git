Printing out Report 2. . . In Progress

Lost track of changes, but now will go and 
make sure that the path will return an array of trains in order.

Created:
1	routeMinutes


Modified:
1	fillMatrix(TRAINS trainList[], TRAINS adjMatrix[100][100])
2
Reasons:
1	Removed extranious comment lines from the bottom of the function.
